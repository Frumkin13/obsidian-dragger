## 1.3.1

- Remove deprecated `setDynamicTooltip` calls
- Remove `getFileByPath` (not available in declared minAppVersion)
- Remove all `!important` and `:has()` from styles.css for review compliance
- Add `.npmrc` with `legacy-peer-deps` to fix npm dependency resolution
- Add automated GitHub Release workflow
