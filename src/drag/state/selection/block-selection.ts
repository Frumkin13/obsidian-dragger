export type {
    BlockSelectionSegment,
    SelectedBlockRange,
} from '../../source/selected-blocks';
export {
    cloneSelectedBlocks,
    groupSegments,
    groupSelectedBlocksIntoSegments,
    isSelectedBlockCoveredByBlocks,
    mergeSelectedBlocks,
    normalizeSelectedBlockRange,
    subtractSelectedBlocks,
} from '../../source/selected-blocks';
