import { describe, it } from 'vitest';

describe('selection-model', () => {
    it('keeps DragSource construction out of selection state', () => {
        // DragSource range construction is owned by src/drag/source/source-ranges.spec.ts.
    });
});
